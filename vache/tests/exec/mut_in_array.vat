fn main() {
    var a: int = 830198309123808;
    var z: [int] = [@a];
    z[0] = 1; // With aliasing, modifying a and z at the same time
    print(z);
    print(a); // Note: the reverse should not be allowed. We shouldn't be allowed to print z and a, since we can't use a until all mutable borrows on it are ended
}
################################
[1]
1
