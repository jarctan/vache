fn main() {
    var a: int = 830198309123808;
    var b: int = @a;
    b = 12; // Modifies b and a
    b = b + 1; // Modifies b and a
    print(b);
    print(a); // Note: in that order, since otherwise we'd use a before all mutable borrows on it end.
}
################################
13
13
